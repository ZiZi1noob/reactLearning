1. Expo vs. Next.js
	a. Expo 是一个用于构建原生移动应用 (iOS, Android) 的框架和平台，它基于 React Native。
	b. Next.js 是一个用于构建网站和Web应用 的 React 框架，专注于服务端渲染和静态生成。

2. JSX (JavaScript XML) vs. TSX (TypeScript XML)
	a. 允许你在 JavaScript 代码中编写类似 HTML 的标签，用于描述 UI 的结构。
	b. 为 JavaScript 添加了静态类型系统，在代码运行前进行类型检查。 

3. useState vs. useReducer

4. useContext

5. useMemo

6. useCallback

7. export vs. export default
	a. export default 用于一个文件只导出一个主要值（如页面组件），导入时可任意命名且不用花括号。
	b. export 用于一个文件导出多个值（如工具函数、组件），导入时必须用花括号按准确名称或别名来引入。

8. 自闭合标签:
<img>		<img src=".jpg" alt="图片">			图像
<input>		<input type="text">				输入框
<br>		<br> 或 <br />					换行
<hr>		<hr> 或 <hr />					水平线
<meta>		<meta charset="UTF-8">				元数据
<link>		<link rel="stylesheet" href="style.css">	外部资源链接
<source>	<source src="video.mp4" type="video/mp4">	媒体源